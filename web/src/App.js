import React from "react";

export function soma(a, b) {
  return a + b;
}

export default function App() {
  return (
    <main>
      <h1>MonoAmbiente</h1>
      <p>CI: npm run build + npm run test (react-scripts)</p>
    </main>
  );
}
